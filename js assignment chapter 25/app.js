function enter(){

    var value = document.getElementById("user")
    var  id = value.value; 
    if(id === "!" || id === "," || id === "." || id === "@"){
        alert("ASCII code of ! is 33 \n ASCII code of, is 44\n ASCII code of. is 46 \n ASCII code of @ is 64")
    } else{
        document.write()
    }
}